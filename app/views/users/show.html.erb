<% provide(:title, @user.name) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
  <div class="span8">
    <% if @user.arguments.any? %>
      <h3>Arguments (<%= @user.arguments.count %>)</h3>
      <ol class="arguments">
        <%= render @arguments %>
      </ol>
      <%= will_paginate @arguments %>
    <% end %>
  </div>
</div>
