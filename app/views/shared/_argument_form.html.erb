<%= form_for(@argument) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :topic, placeholder: "Compose new argument title" %>
  </div>
  <%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>